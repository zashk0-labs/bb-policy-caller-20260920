name: Old alias unchanged secret-passing workflow

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Invoke transferred action through old local-looking alias
        uses: zashk0-labs/bb-policy-action-20260920@master
        env:
          POLICY_ALIAS_SENTINEL: ${{ secrets.POLICY_ALIAS_SENTINEL }}
